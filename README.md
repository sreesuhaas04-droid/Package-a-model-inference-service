# Package-a-model-inference-service
A Model Inference Service is an application that exposes a trained machine learning model through an API so that other applications or users can send input data and receive predictions in real time. Instead of running the model manually in a notebook, the model is packaged into a web service that can be deployed and accessed over HTTP.
